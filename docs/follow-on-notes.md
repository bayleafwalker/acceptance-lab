# Suggested Kotona follow-on map

Do not add links from the root note until the target content exists and passes
site validation.

| Proposed slug | Working title | Evidence needed before publishing |
| --- | --- | --- |
| `a-correct-result-can-still-have-a-forbidden-trajectory` | A correct result can still have a forbidden trajectory | One live trace where output quality and trajectory acceptance disagree |
| `the-platform-can-run-the-scorer-the-domain-still-owns-the-rubric` | The platform can run the scorer; the domain still owns the rubric | A scenario authored or reviewed by someone other than the harness implementer |
| `authorization-is-not-business-authority` | Authorization is not business authority | A concrete identity/delegation flow plus the business decision it cannot settle |
| `the-benchmark-needs-a-promotion-rule` | The benchmark needs a promotion rule | One model, prompt, retriever, or harness comparison that leads to adopt, hold, or rollback |
| `acceptance-lab-first-live-candidate` | Acceptance Lab: first live candidate | A traced non-fixture run, schema gaps, and a reproducible report |

When each note lands:

1. add the new slug to the root note's `relates` list;
2. add the root note to the new note's `relates` list;
3. replace the plain-text item in the root note with an actual link;
4. update the project page only after the capability is demonstrably current;
5. keep failed or superseded results in lifecycle history rather than deleting
   the path that changed the design.
